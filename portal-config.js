/* Supabase Dashboard → Project Settings → API.
   Paste ONLY the Project URL and publishable key (legacy anon key also works).
   Never paste the database password or service_role key here. Database access is
   protected by the Row Level Security policies in supabase/schema.sql. */
window.STEM_CLUB_SUPABASE = {
  url: 'https://dwiftepdopslglriwkgh.supabase.co',
  anonKey: 'sb_publishable_Kv9zckv53THCl4p_PGBH7A_VZh1Xe_i'
};
